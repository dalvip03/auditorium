[project]
name = "auditorium"
version = "0.1.0"
description = ""
authors = [
    {name = "Pradhnesh",email = "pradhnesh_dalvi@mail.mumbai-tech.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django (>=6.0.4,<7.0.0)"
]

[tool.poetry]
packages = [{include = "auditorium", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "djhtml (>=3.0.11,<4.0.0)"
]
